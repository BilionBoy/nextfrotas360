<%= render "shared/context_nav", title: t("activerecord.models.#{GCategoriaCarteiraCondutor.model_name.i18n_key}.other") do  %>
  <span class="breadcrumb-item"><%= link_to t("activerecord.models.#{GCategoriaCarteiraCondutor.model_name.i18n_key}.other"), g_categorias_carteiras_condutores_path, class: "text-dark" %></span>
  <span class="breadcrumb-item active"><%= t("helpers.submit.#{params[:action]}") %></span>
<% end %>
<%= simple_form_for(@g_categoria_carteira_condutor) do |f| %>
  <div class="card">
    <div class="card-header">
      <h5 class="my-0">
        <%= @g_categoria_carteira_condutor.new_record? ? "Incluir " : "Editar " %><%= t("activerecord.models.#{GCategoriaCarteiraCondutor.model_name.i18n_key}.one") %>
      </h5>
    </div>
    <div class="card-body">
      <%= f.error_notification %>
      <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
      <div class="row">
        <div class="col-3">
          <%= f.input :descricao, input_html: { class: "capitalize"}, placeholder: "Descricao"  %>
        </div>
      </div>
    </div>
    <div class="card-footer">
      <%= btn_submit(f) %>
      <%= link_to "Voltar", g_categorias_carteiras_condutores_path, class: "btn btn-light" %>
    </div>
  </div>
<% end %>